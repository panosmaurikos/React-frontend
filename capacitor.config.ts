import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: '@minimal/material-kit-react',
  webDir: 'build'
};

export default config;
